services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ninjatracker-frontend
    ports:
      - "5173:5173"
    environment:
      VITE_API_BASE_URL: http://host.docker.internal:4000
    stdin_open: true
    tty: true
    restart: unless-stopped